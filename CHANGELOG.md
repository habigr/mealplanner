# MEAL PLANNER — CHANGELOG

Full version history, DONE log, and the overnight-run summary. CLAUDE.md keeps only the last few versions + a pointer here. Newest first.

---

## VERSION HISTORY

- v162 — **Fix: mobile couldn't actually update (version banner had no update action).** The "Update available" banner only had a Dismiss button and told you to "hard refresh" — impossible to do easily on a phone, so the notification kept showing but never updated. Added a one-tap **Update** button → `forceAppUpdate()`: clears Cache Storage, unregisters any service workers, and reloads via a fresh cache-busting URL (`?v=<ts>`). Banner now: **Update** / **Later**. node --check pass.
- v161 — **Edit-screen refinements + view is now read-only.** (1) **View = read-only** — removed the tap-to-edit grocery-name (and its hint) from `renderGroupedIngredientsHtml`; ALL editing happens in edit mode now (Robbie). (2) Dropped the big **"Edit"/"Add" title + subtitle**; the **dish name is the title** (22px, borderless, accent underline on focus). (3) **Sticky Original/Adjusted/Unit column headers** while scrolling the ingredient list. (4) A touch more **contrast on mobile top fields** (link/serves/cooking-for/notes get a subtle fill). (5) **Consistent, view-matched type scale** on desktop (inputs 13.5px, labels 10.5px) so sizes stop feeling jumpy; mobile keeps 16px inputs (iOS no-zoom). CSS + small view-render tweak. node --check pass. Kept view mode (Robbie: its sorted-ingredients + steps are useful) — next: restyle mobile bottom tabs, carry this look app-wide, polish the view. Backup chain from `pre-editview-merge`.
- v160 — **Edit screen restyled to look like the view (Stage 1 of edit=view merge).** Calm, view-like dish edit modal — the first step toward retiring the separate read-only view. Changes (mostly CSS, scoped to `#dishModal`): fixed the "gray everywhere" (the shared `.modal` uses a translucent glass bg that read gray once fields went transparent → edit modal now a **solid off-white canvas**); fields render as clean **underlines** (accent underline on focus; notes keeps a light box); **each ingredient is its own pane** (card + shadow) with a subtle **wiggle on hover** (hover devices, reduced-motion safe); **grocery-name/section decluttered** — shown as accent chips when set, hidden until row-hover when empty (compact-always on mobile); **recipe image hero** at the top like the view (`setDishModalHero`); **"Going out" is now a small pill next to a small Cook pill** (`dishActionRow`, `syncGoingOutBtn`; hidden checkbox retains logic); when **editing an existing dish**, the Day/Meal picker + import chrome + "Recipe Details" heading are hidden (`editing-existing` class); removed the odd gray/dashed on "Recipe serves"; **thin rounded scrollbar** so all modal corners stay rounded; ingredient header subtitle "Final list — drag to reorder"→"Drag to reorder". Also: **removed the Cook button from meal-plan dish cards** (`planCookBtn`) — cook only from the dish detail/edit screen (cooking mode de-emphasized). Mobile-first throughout (underlines not gray fills, panes, hero 150px, wrapping actions). **Backup tag `pre-editview-merge` + `_backups/index_v159_pre-editview-merge.html`.** node --check pass. NEXT: reroute taps to this screen + retire `showDishDetail`/`showRecipeDetail` (Stage 2).
- v159 — **AI chat sees each dish's exact ingredients (stops guessing/duplicating).** A transcript showed the assistant editing blind — it only had the grocery summary, so it asked the user to list ingredients, mis-edited, and created duplicates. Fixes: (1) **Full per-dish ingredients in context** — `getAiChatContext` now lists every dish's ingredients with exact name + `(section: …; grocery: …)`. (2) **Forgiving `edit_ingredient` match** — normalizes and strips `[bracket]` tags so "cake mix" matches "Cake Mix [cake]" and *edits* instead of duplicating. (3) **Prompt guidance** — use the visible ingredients (never ask the user to list them), act in one shot, and the bracket-cleanup pattern (`newName` clean + `component` = the tag), never re-add existing items. Enables the "strip [icing]/[cake] from names → move to section" task to complete correctly in one pass, and lets it clean up prior duplicates. Tradeoff: more tokens/message on big trips (Sonnet). **Backup tag `pre-v159-ai-visibility`.** node --check pass.
- v158 — **AI chat can now add ingredients + edit section/grocery-name; knows every field.** Fixes two reported gaps. (1) **Add ingredients** — new `add_ingredient` action (`executeAddIngredient`) takes a list `["2 lbs clams","4 lobster tails"]` or a single item and pushes them into the dish (was impossible — `edit_ingredient` only edited existing, so "add clams" failed). `edit_ingredient` also now **adds** an ingredient it can't find instead of erroring. (2) **Section + grocery name editable** — `edit_ingredient` now handles `component` (recipe section), `groceryName` (purchase name; carries store via `noteGroceryRename`), and `groceryDept`; fixed the prompt bug that told it to set grocery name via `newName` (which renamed the ingredient). (3) **"Knows everything"** — added a **DATA MODEL** block to the system prompt enumerating every dish/ingredient/grocery/trip field + the action that changes each, and "don't say you can't." Actions re-render + rebuild the grocery list. **Backup tag `pre-v158-ai-actions`.** node --check pass.
- v157 — **Cloud fetcher live + entity-decode fix.** `fetchRecipe` deployed & verified: **exact imports work** for normal recipe sites (e.g. cookieandkate.com returned exact name/servings/ingredients/steps). Confirmed limits: **Serious Eats / Allrecipes / Dotdash return HTTP 402 — they block Google Cloud IPs** (not just UA), so link import can't beat those without paid residential proxies; the app already routes them to Paste/📷 with a clear message. JS-rendered sites (no static JSON-LD, e.g. loveandlemons) fall through to the AI/paste path. **Fix:** `decodeEntities` now handles named vulgar-fraction entities (`&frac12;`→½, etc.) + hex numeric + common punctuation — recipe fractions were showing raw; applied in both the function and the client backup extractor. **Requires a redeploy** (`firebase deploy --only functions`) to activate the fraction fix. node --check pass (app + function).
- v156 — **Reliable, integrity-safe recipe import via a server-side fetcher (Cloud Function).** New `functions/fetchRecipe` (Firebase, Blaze) fetches the page with a browser User-Agent (gets past the bot-blocking that stops browser/proxy fetches on Serious Eats/Dotdash) and extracts the page's own **schema.org JSON-LD recipe deterministically — ZERO AI, exact quantities, never fabricates** (returns `{error}` if no real recipe). Import chain in `backgroundImportAndReview`: **Cloud Function (exact) → client-proxy JSON-LD backup → Jina+AI (strict, no-invention) → clear failure** that names known blocker sites and points to Paste/📷 (also fixes the misleading #12 wording). Extractor verified against real-world shapes (`@graph`, `HowToStep`, `HowToSection`, entity-decode, image array/object). App **degrades gracefully** — until the function is deployed it behaves like before. New files: `functions/index.js`, `functions/package.json`, `firebase.json`, `.firebaserc`; `RECIPE_FETCH_FN` constant in index.html (confirm URL after deploy). **Backup tags `pre-v155-import-jsonld`, `pre-v156` (via pre-v155).** node --check pass (app + function). **NEEDS `firebase deploy --only functions` to activate.**
- v155 — (folded into v156) client-side multi-proxy JSON-LD attempt — superseded by the Cloud Function as primary; kept as a secondary fallback.
- v154 — **Smarter AI chat: Sonnet + formatting + it understands the whole app.** (1) **Model → `claude-sonnet-4-6`** for the chat (better planning/scaling/ideas); batch jobs (grocery cleanup, consolidation) stay on cheap Haiku. (2) **Renders formatting** — `_mdToHtml` shows **bold** + bullet lists (removed the old strip-and-forbid); prompt now permits simple bold/bullets. (3) **Consolidation is explainable** — `getAiChatContext` now feeds each grocery line's store, your +/- adjustment, and for combined lines the **per-form breakdown** (`Limes: 5 [combined: 2 cups juice→~4, 1 oz zest→~1]`), and the cap rose 25→60. So "why 5 limes?" gets a real answer. (4) **New "How this app works" system section** — grouping-by-grocery-name, consolidation, adjustment delta, components (sauce/main), store memory — so it can discuss + operate all the new features. (5) **Smart scaling** guidance (round to buyable amounts, leave "to taste", spices sub-linear, whole counts stay whole) via `edit_ingredient`. (6) **Real vegetarian swaps** (chicken→chickpeas/tofu, beef→lentils…) honoring trip restrictions, not just a flag toggle. Chat logic otherwise unchanged; actions/undo intact. Higher per-message cost (Sonnet) — accepted. **Backup tag `pre-v154-smart-chat`.** node --check pass.
- v153 — **AI chat modernized (A1).** Visual/UX redesign, no chat logic touched. (1) **Chat-bubble icon** throughout (Robbie's ask) — FAB, header avatar, and beside every assistant reply (iMessage-style avatar via a data-URI ::before). (2) **Full-width composer** — the input row was crushed by buttons; now a big input with a **＋** import button left + **↑** send right. (3) **Build Recipe moved out of the input row** into a welcome **quick-chip**; the confusing 📷 (it's recipe-import, not a camera) is now the ＋ attach + an "Import from photo" chip. (4) **Smart prompt chips** under the greeting (🍽️ Dinner ideas · 🛒 What am I missing? → `aiAsk()` sends them · ✨ Build a recipe · 📷 Import from photo); persisted through Clear via `_aiQuickChips()`. (5) **Cleaner header** — "Meal Assistant" + green online dot, Clear as a subtle icon, dropped the "Powered by Claude" subtitle. (6) **Mobile full-height sheet** so the keyboard can't crush it. (7) FAB lost the dated pulsing glow → clean navy circle with hover lift; flatter bubbles/panel. **Backup tags `pre-v153-aichat-polish`.** node --check pass. (Logo: used a clean bubble SVG; can match the credit-card-app bubble exactly if Robbie shares it.)
- v152 — **Cross-unit consolidation via AI (the "5 limes" payoff).** A merged mixed-unit line (see v151) is resolved to ONE buy-count by a **cached, batched Haiku call**: `runGroceryConsolidation` sends all mixed lines in a single request and gets back `{buyQty, buyUnit, parts, note}` per line, stored as `groceryMeta[key]._consol` keyed on a **source signature** (`_consolSig`) so it never recomputes unless the source amounts change. `aggregateRows` uses the cached buy-count as the line total when the signature matches (else flags `needsConsol`); `buildGroceryList` triggers the pass once for unresolved lines then re-renders (guarded against loops). Falls back gracefully with **no API key** (shows the parts pile) — and any unresolved mixed line now shows a **"· tap to set amount"** hint → item sheet. The signed +/- adjustment rides on top of the AI count. Cost: fractions of a cent, once per changed line. Confirmed working by Robbie. **Backup tag `pre-v152-ai-consol`.** node --check pass.
- v151 — **Grocery consolidation: merge lines by purchase name.** The aggregation key dropped **unit** (was name+unit) so all lines sharing a grocery name collapse into one (lime juice + lime zest + whole lime, all named "Lime" → one "Lime" line). Merge is driven purely by the grocery name you set (with v144 reuse-suggestions), so nothing merges you didn't co-name. Quantities are bucketed per-unit: same-unit contributions sum ("4 cloves"); mixed units show as parts ("2 cups + 1 oz + 1 whole") until the v152 AI resolves them. Rides on the v150 migration layer so re-keying preserves store/bought/adjustment. **Backup tag `pre-v151-consolidate`.** node --check pass; merge verified on a throwaway trip.
- v150 — **Grocery key-migration layer: renaming an item keeps its store + bought + adjustment (#10 Stage 0).** Renaming/editing an ingredient re-keys its grocery line; `reconcileGroceryMeta` used to just delete the orphaned meta, dropping the store/checkoff. Now: (1) `reconcileGroceryMeta` **migrates** an orphaned line's store/bought/adjustment/dept to the single live line with the same normalized purchase name (or the rename target — see below) instead of deleting, only when unambiguous and only into fields the target lacks. (2) `noteGroceryRename(old,new)` records the rename (old→new norm map, consumed by the migration) and carries the line's **resolved** store (incl. auto-assigned/remembered, read from live rows) to the new name's memory. Wired into both rename surfaces (recipe/dish view `editGroceryName`, edit-modal `editRowGroceryName`) plus a pre-save sweep `_noteRowRenamesBeforeSave` that catches ingredient-**text** renames in the edit modal (via a per-row `origItem` snapshot). Verified on a throwaway trip: store + checkoff survive a rename. Strictly improves on the old always-delete (ambiguous cases fall back to prior behavior) → FIP-safe. **Backup tag `pre-v150-key-migration`.** node --check pass. This is the safety prerequisite for the cross-unit consolidation (next).
- v149 — **`sources[]` backbone (consolidation groundwork, additive/FIP-safe).** Each aggregated grocery line's contributor list (`usedFor`) now also carries the **recipe item name** + **component** per occurrence, and is exposed as `row.sources` ({dishId,item,component,quantity,unit}). Plumbing only — nothing visible changes; it's the provenance the upcoming cross-unit consolidation (group by purchase name → AI unit math) and the item-sheet breakdown read. **Backup tag `pre-v149-sources`.** node --check pass. NEXT (careful, throwaway-trip): key-migration layer (reconcileGroceryMeta MIGRATE not delete) → group-by-purchase-name + AI conversion.
- v148 — **One edit path: grocery-list dish pills are display-only.** The dish pills on the grocery List used to open the old per-ingredient editor (`pickDishIngredient`) in a separate window. Removed that — pills are now just labels. Editing happens only by tapping the **item name** → the item sheet → tap a dish → its full editor. Render-only, FIP-safe. node --check pass.
- v147 — **Recipe components (sauce / main sections).** Additive, no migration (empty section = today's behavior). (1) **Editor:** each ingredient row gets a **📂 Section** control beside the 🛒 grocery name (`editRowSection`) — free text with suggestions of the dish's existing sections (`getModalSectionNames`); stored as the ingredient's `component` (carried through the pipeline since v145). (2) **View:** when a recipe/dish has **2+ sections**, `renderGroupedIngredientsHtml` groups ingredients under their real section headings (first-appearance order; unassigned → "Other"); 0–1 sections stays a flat list. Same ingredient can appear in two sections (two rows). (3) Made `promptAction` support `allowEmpty` so the grocery name / section can be **cleared** (empty OK now returns '' instead of being treated as cancel) — applied to `editRowSection`, `editRowGroceryName`, `editGroceryName`. Deferred: AI-import section detection + cooking-mode grouping. **Backup tag `pre-v147-components`.** node --check pass; verified 25/25 (v139→v146).
- v146 — **Edit/view typography + card cohesion (CSS-only, FIP-safe).** Aligned the dish edit modal to the clean view look and unified the type hierarchy. `.detail-card` lost the heavy `panel-warm` box → light card (`--panel` + `--border-light`). One consistent type scale across edit + view: **section headers 12px bold uppercase** (`.detail-card-header strong`, `.final-details-heading`, and the view's `.recipe-section-heading` all matched), **field labels 10–11px uppercase muted**, **inputs 14px** — including the ingredient-section inputs (were 12.5px, the odd one out) and its column labels (were 9px). Fixes the "various font sizes / headers smaller than the inputs" incoherence. **Backup tag `pre-v146` (use `pre-v145-ingredient-pipeline` chain).** node --check pass.
- v145 — **Dish edit modal cohesion: grocery name in edit (fixes data loss) + declutter + font consistency.** (1) **Fixed a real data-loss bug:** the edit pipeline (`addIngredientRow`→`collectIngredients`→save) never carried `groceryName`, so editing+saving a dish silently wiped every grocery name. Now `groceryName` (and `component`, for future sections) ride through the whole pipeline; `normalizeDish` carries `component` too. (2) **Grocery name now editable IN the edit modal**, nested under each ingredient name (a tappable "🛒 [name]" line, aligned on desktop + mobile via an `.ing-item-cell` wrapper), reusing the same suggestions prompt (`editRowGroceryName`). So grocery name is consistent across view + edit. (3) **Removed clutter:** the Cook/owner dropdown, the Vegetarian checkbox, and the ✦ AI Check (`aiCheckVegetarian`) button — kept "Cooking for" (scaling). `isVegetarian` is preserved on save (not wiped now that the checkbox is gone). (4) **Font normalization** scoped to `#dishModal`: labels 11px, inputs 14px, compact ingredient inputs 13px. Touches the save pipeline but only *preserves* more data → FIP-safe. **Backup tag `pre-v145-ingredient-pipeline`.** node --check pass. (Verify on a throwaway trip: grocery name + veg flag survive an edit+save.)
- v144 — **Grocery-name suggestions (reuse existing trip names) — in the existing editor, no new place.** When you set an ingredient's grocery-list name (tap an ingredient in the dish/recipe view), the prompt now suggests **every purchase name already in the trip** (`getTripGroceryNames()`, deduped via `_groceryNameNorm`), so you reuse "Lime" instead of spawning "Limes" (which wouldn't consolidate). Added via a `suggestions` option on the shared `promptAction` (datalist) — reused, not a new surface. (Briefly tried a "Buy as" field on the grocery sheet; removed it — Robbie: don't add new edit places, edit at the entry point.) Manual items get the same suggestions on their name. FIP-safe; no AI. **Backup tag `pre-v144-buyas-suggest`.** node --check pass. KNOWN: view/edit ingredient editing is still split (amount/name in the dish edit modal, grocery name in the detail view) — the cohesion fix is next.
- v143 — **Grocery item sheet (brief 4b): tap-a-line breakdown with dish jump-back + signed-delta adjustment.** Tapping a grocery line opens a calm breakdown sheet (replaces the old multi-field editor — concept reduction). Recipe-sourced: header (name + "used in N dishes"), a **"From your dishes"** list (each dish + its contribution), a **tally** (From dishes + Your adjustment = Total to buy), and a **signed ± adjustment** (a delta starting at 0 — total to buy = recipe total + your adjustment, so it rides along as dishes change; stored as `groceryMeta.buyDelta`; legacy absolute `buyQty` still honored + migrated on edit). Tapping a dish opens its **full meal-plan editor**, and **saving returns you to that grocery item's sheet** (`_returnToGrocerySheet` + `closeDishModal` hook) so you can keep adjusting. ± is **unit-aware** ("2 tbsp" → `− 2 + tbsp`); mixed-unit lines fall back to a typed total. Manual items: name + qty + remove. No store/dept/name editing in the sheet (those live on the row / in the dish). Additive meta + existing save paths → FIP-safe. **Backup tag `pre-v143-grocery-sheet`.** node --check pass. NEXT: Item 3 = cross-*item* consolidation (Limes + lime juice + zest → one "Limes" record with AI unit conversion) — the data-model build this sheet is the home for.
- v142 — **Nav polish: Recipe Library launcher + Trip Settings fix + mobile header definition + Trip Settings restyle.** (1) **Recipe Library** is now a global **📖 launcher** in the header right-cluster (labeled pill on desktop, icon-only on mobile) → opens the existing full-screen library overlay from inside a trip too (was only reachable from the home screen; the in-trip Recipes page was dead). It's deliberately NOT a trip tab (the library is cross-trip). Home-screen button kept. (2) **Trip Settings bug fixed** — it was blanking every page on open, so closing the drawer stranded you on a blank screen. Now it's a **pure launcher**: opens OVER the current page, never blanks it, not persisted as `lastTab` (and `restoreLastTab` maps a stale `setup`→`planner`). **Desktop = centered window; mobile = the sliding pane** (kept). (3) **Mobile header definition** — soft drop-shadow so the flat bar reads as an app bar; header icon buttons restyled for the flat (non-photo) header. (4) **Trip Settings panel restyled** into clean grouped cards (Details / Stores / Who's here / Share / Activity / Manage), sentence case, consistent secondary buttons, tidier trip-code/share block; renamed "Trip Details"→"Trip settings". Nav/CSS only → FIP-safe. **Backup tag `pre-v142-nav-library`.** node --check pass.
- v141 — **Grocery brief v2, step 1: removals + flat minimal header (4c + 4d).** Presentational/FIP-safe. (1) **Removed the ✦ grocery button** (`runGroceryDedup` "AI: find duplicates") — covers both the "dup button" and "AI button" (same control); functions left parked; retired the auto dedup-banner render in `renderGroceryTable`. Identical-name dups are already handled by groceryName normalization at import (cross-unit consolidation is the separate G3 build, still TODO). (2) **Removed the header Save icon** — saves are automatic. (3) **Scrapped the v138 gradient banner + color-scheme picker** for a **flat minimal/light header**: light surface (`--panel`), dark title, muted dates, thin accent rule; removed the `--hg` gradient + `header::before` overlay + the v135 cinematic vignette (which read as a gradient on a flat bar). **Merged the header + the separate tab strip into ONE slim ~62px bar (Option A)** — `‹ Trips` · trip name + dates inline · section tabs (pill style) · online/profile icons; dropped the redundant "Meal Planner" eyebrow and the gear icon (the Trip Settings tab covers it). On mobile the tabs/back hide (bottom nav handles them) and the header is a compact name+dates+icons bar. Removed the Color Scheme dropdown from Trip settings; `applyTheme` now applies one professional navy accent app-wide (scheme classes/`mapTheme` left defined; trip-card accent standardized to navy). (4) **Mobile title clip fix** — wider title max-width (freed by removing the Save icon) + larger font so trip names fit. **Backup tag `pre-v141-header-removals`.** node --check pass. Next per plan: rebuild the grocery item sheet to brief 4b (dish-grouped breakdown + jump-to-dish + single "adjust total"), seeding the 4a design tokens + mobile grocery cleanup.
- v140 — **Grocery refinement UX: persistent sub-tabs (no "Done" button) + one unified tap-name Item editor (#36).** Robbie: grocery felt clunky — too much screen-hopping, and you couldn't edit an item end-to-end from one place. (1) **Sub-tabs always visible:** moved the **List · Assign Stores · Shop** segmented control OUT of the workspace card (which Shop hid), so all three are persistent tabs you switch between by tapping; **removed the "✕ Done" button** (Shop is just another tab now). **Assign Stores mode is unchanged** — still the bulk-assignment grid (Robbie: keep it, it's the best place for bulk trip assignment). (2) **Tap an item name ANYWHERE (List, Shop, AND Assign) → the same bottom "Item sheet"** (`openGroceryItemSheet` / `saveGroceryItemSheet`) — consistent everywhere (Robbie's ask). Everything is editable **inline in that one window** (no separate "Edit" click): **item name**, **per-recipe amount + unit** (one row per contributing dish), **store**, **department**, and a single **Bought / got it** toggle. One **Save** writes the amount/name back to the meal-plan dish ingredient(s) (`qtyOverride`/`unit`/`groceryName`) AND the line meta, then re-aggregates to the final list — so a refinement (substitute, qty/name/store tweak, mark bought) happens in place, plan→list, without bouncing into Assign mode. Manual items edit name/qty/unit too. **Dropped the Need/Have/Bought 3-way** (Robbie doesn't use "Have" — he assigns to an "already have" store) → just a Bought toggle. A rename changes the aggregation key, so the store is also written to the v139 memory under the new name so it survives the re-key. Mobile-first: slide-up bottom sheet, generous spacing, big tap targets, safe-area padding (Robbie can't test on mobile, so built clean by design). Reuses existing save paths → no new write logic, no data reshaping → FIP-safe. Sets up G3 (this sheet is where the lime consolidation math will live). **NOT added to the meal-plan tab** (deferred — avoid two competing edit surfaces; easy fast-follow). **Backup tag `pre-v140-grocery-ux`.** node --check pass; no dup defs. **⚠️ Verify on a THROWAWAY trip + regression 25/25 before push.**
- v139 — **Grocery G1: one-off store auto-assignment + per-name store memory (closes #10).** From the grocery brief (BACKLOG "GROCERY & RECIPE COMPONENT PROGRAM"). (1) **`guessStore` now honors `storeRules`** (Robbie's call) — first matching App Admin keyword rule wins (`_matchStoreRule`), then the dept→store guess, then Stop & Shop; the "Store Auto-Assignment Rules" UI was previously **dead code** (guessStore ignored it). (2) **Per-name store memory** — new `currentEvent.tripMemory.storeMemory` map (`_groceryNameNorm(name) → store`), written whenever a user assigns a store (`setItemStore`, `assignRowToStore`, new `setRowStore`). Stored under `tripMemory` (NOT `groceryMeta`) so `reconcileGroceryMeta` can't delete it. (3) **`aggregateRows` auto-resolves** a line with no explicit assignment via `autoStoreFor` = memory → keyword rule → produce/meat dept (`DeCiccio's`); unknown one-offs return `''` and stay Unassigned. **Display-only — never written to `groceryMeta`** — so a renamed/edited item (which changes its `normKey`) re-resolves its store from memory instead of `reconcileGroceryMeta` silently dropping it. **This is the #10 fix.** Explicit user choices (any stored `meta.store`, including a deliberate "Unassigned") are never re-guessed. (4) **Shop mode pins "Needs a store"** (Unassigned) to the TOP and relabels it (section + chip). (5) **Inline store `<select>` per row in List mode** (`setRowStore`) to reassign a one-off without entering Assign-Stores mode (Shop-mode inline reassign deferred to v140 to avoid risking the in-aisle check-off label). Additive only (`tripMemory.storeMemory` + display fields); no node reshaping, no new `groceryMeta` writes from the resolver → **FIP-safe**. Also bumped the stale `APP_VERSION_TAG` (was still v137). **Backup tag `pre-v139-grocery-g1`.** node --check pass; no dup defs; +56/−8. **⚠️ DATA-SENSITIVE (grocery) — verify on a THROWAWAY trip + regression 25/25 before push; never test against FIP.**
- v138 — **Color-scheme header — replaces the entire photo-banner system (Robbie 2026-06-27; NO photos).** Robbie didn't want header photos at all. Removed ALL photo machinery: `getThemeGallery`/`setThemeGallery`/`getDefaultThemeGallery`/`getActiveThemeImage`/`applyThemePhotoFromTrip`/`applyThemeGalleryImage`/`applyCustomHero`/`applyHeroPos`/`testImageUrl`/`renderThemeGallery`/`selectThemeImage`/`removeThemeImage`/`addThemeImageUrl`/`updateThemePreview`/`handlePreviewCropChange`/`switchThemeAdminTab`, the `GALLERY_KEY` localStorage key, the `themeAdminTab`/`themePreviewIdx` globals, the whole **"Themes & Photos"** App Admin tab + its panel + dead `.theme-preview-*`/`.theme-gallery-*`/`.theme-crop-*` CSS, every `themePhoto` read/write, and the header's photo `background-image:var(--hi/--dhi)` + `--chi/--chip/--hp` vars. Added: an **8-option Color Scheme dropdown** in Trip settings (Midnight · Ocean · Forest · Sunset · Plum · Slate · Cherry · Pride), saved on the trip via the repurposed `currentEvent.theme` → **syncs to all users on the trip** (trip-level, consistent on every device). Each scheme defines the app accent palette (`--ta/--tad/--tas/--tam/--tw/--tb`) so it tints the **whole app** (buttons/active states), and a header **gradient** (`--hg`); Pride is a rainbow header with one clean violet accent. Header now shows trip **name + dates**. Readability preserved by the existing `header::before` light/dark overlays (no overlay change needed). Back-compat: `mapTheme()` maps legacy `larchmont→midnight`, `fire-island→ocean`, `adirondacks→forest` (and any unknown → midnight) at render time — old trips are NOT rewritten. New-trip + normalize default is now `midnight`. **FIP-safe:** only writes the trip's scheme field (already part of `currentEvent`); the orphaned `themePhoto` field left on existing Firebase trips is harmless (no longer read). **Backup tag `pre-v138-photo-removal`** = one-command rollback. node --check pass (extracted JS); no duplicate defs; file −57 net lines. Browser regression (25/25) + 2-device scheme-sync + visual check pending Robbie.
- v137 — **Fix: stop auto-opening a finished/archived trip on launch (land on home instead).** A user reported the app kept "defaulting into FIP" every launch instead of showing home with FIP in Archived. Cause: opening a trip sets the URL hash to `#trip={code}`; on each launch `startAppAfterAuth` re-reads that stale hash and `loadTrip()`s it BEFORE `renderLanding()` — so a finished trip yanked the user straight in, bypassing home + the Archived section. Fix: at launch, if the hashed trip is a PAST trip (cached `endDate` > 1 day ago), don't auto-open it — clear the stale hash and fall through to home, where it shows under Archived. Active/future trips and fresh join-links still open normally. Launch-behavior only, no data/sync/Firebase writes → FIP-safe. node --check pass. **NOTE:** this v137 = the launch fix, shipped from the v136 base; the *separate* v137 banner-gallery build (#28) was parked on branch `banner-wip` to keep this fix isolated from the untested banner — it becomes **v138** when 2-device tested.
- v136 — **More Recipe Library sorting (#35), unified with the bank sort.** Added a **Sort** dropdown to the Recipe Library overlay: Recently added / Name A–Z / Most used / Top rated / **Quickest to cook** (new). Refactored to a single shared `sortRecipesForLibrary()` used by BOTH the library overlay and the recipe bank (the bank's old inline `switch` now calls it). `_recipeSort` now **persists per-device** (localStorage) and `setRecipeSort` refreshes both views. Caught + fixed a **duplicate-definition clash**: a `setRecipeSort`/`_recipeSort` already existed for the bank — aligned on the existing `'alpha'` key instead of adding a parallel one. Render-only, no data/sync → FIP-safe. node --check pass.
- v135 — **More polish (CSS-only, FIP-safe): frosted toasts, smoother tabs, cinematic banner vignette, living hero.** (1) Toasts are now dark frosted-glass (`backdrop-filter` blur + translucent) with a deeper shadow; success/error tinted translucent. (2) Page/tab switches use a slightly longer, springier `pageFade`. (3) The trip **banner** gets a bottom **vignette** (`inset` box-shadow) for depth + title legibility — deliberately does NOT touch the per-theme image or `--chip/--hp` positioning. (4) The landing hero's decorative rings slowly **drift/pulse** (purely decorative, `pointer-events:none`). All respects `prefers-reduced-motion`. No JS/data/sync. (Banner *image* upgrade = the separate #28 shared-gallery-sync build; this is just the treatment.)
- v134 — **Modern/futuristic polish pass — glass + glow + motion (additive CSS only).** A `v134 polish` block appended to the stylesheet, layered over the existing design system (reverts cleanly). (1) **Frosted glass** on the floating chrome — modals, App Admin panel, side drawer, and the mobile bottom-nav get `backdrop-filter: blur + saturate` + a translucent `--glass` surface (light/dark variants) and a hairline border; the modal backdrop gets a light blur. Graceful fallback to a ~70% opaque panel where `backdrop-filter` isn't supported. (2) **Glow + press** — soft accent-tinted resting glow on buttons (secondary/ghost/icon excluded), tactile `:active` press; the landing "New Trip" button keeps its navy glow (not theme-accent, so it can't go teal). (3) **Motion** — trip cards lift + glow on hover and fade-rise in with a slight stagger; modern `:focus-visible` rings. Respects `prefers-reduced-motion`. No JS/data/sync changes → FIP-safe. **Backup tag `pre-polish-v133`** created for one-command rollback. node --check pass; visual verification on Robbie's devices pending.
- v133 — **Import flow reworked to Robbie's model: import LOADS details into the editor; the single editor Save is the only write (#3).** Supersedes v132. Previously the review panel's "✓ Save to Library" (`confirmRecipeImport`) saved to the library immediately, then the still-open dish modal's Save saved a 2nd copy. New model (Robbie): the review panel's primary button is relabeled **"Save Details"** and routes through `editRecipeImport`, which loads the extracted recipe into the editor (bankOnly mode) WITHOUT saving — the user reviews/edits, and the editor's **Save** (`saveDishFromModal`) is the ONE write to the library. Removed the redundant "✎ Edit" button (the primary now does that) + added a helper note ("review and edit, then tap Save"). Kills duplicate imports by construction (single save path) and lets the user edit before committing. Applies to photo + AI-bot imports (both use the review panel via `showRecipeReviewPanel`). `confirmRecipeImport` now has no callers (left as harmless dead code). node --check pass; verify on a throwaway import. NOTE: URL import uses a separate prompt (`saveImportedToLibrary`) — not yet aligned to this model (candidate follow-up).
- v132 — **Fix: importing a recipe from a photo no longer saves it twice (#3).** The photo importer is a tab INSIDE the Add-Recipe dish modal. `confirmRecipeImport` saved the extracted recipe to the library AND then pre-filled the still-open dish modal — so tapping the modal's Save (`saveDishFromModal`) saved it a SECOND time with a fresh id, and the library dedup missed it (the photo import's `source:'photo'` didn't match the modal save's empty source, so name+source didn't match). Fix: after `confirmRecipeImport` saves the recipe, if the dish modal is open it now CLOSES it instead of pre-filling (also clears `_pendingLibraryRecipe`/`_pendingBankRecipeId`), so there's no second Save to create the duplicate. Editing before save is still available via the review panel's "Edit" button (`editRecipeImport`, which pre-fills WITHOUT a pre-save → single save). node --check pass; manual verify on a throwaway import recommended.
- v131 — **Fix: image-search API keys now sync across devices like the Anthropic key (#29).** `loadSharedApiKey` was bidirectional for the Anthropic key (pull the key down from the trip AND push a local-only key up to the trip → self-heals across devices) but only PULLED for the image keys (Spoonacular/Google/Pexels). So image keys entered on one device — or before v110, or from the landing App Settings with no trip open — stayed local-only and never reached the user's other devices (Robbie's report: "only the Anthropic key saves across devices"). Added the matching push-up: on trip open, any image key sitting in localStorage that the trip lacks is written to the trip (`currentEvent.sharedImageApiKey`/`sharedGoogleKey`/`sharedGoogleCx`/`sharedPexelsKey` + `commitAndSync`), mirroring the Anthropic self-heal. Additive trip fields, `validateBeforeFirebaseWrite` still applies → FIP-safe. node --check pass (manual test skipped at Robbie's request).
- v130 — **Fix: un-archive now sticks + archive syncs across devices live; "New Trip" button no longer goes teal.** (1) **Un-archive wasn't holding** — un-archiving a PAST trip got silently re-archived by `autoArchivePastTrips` on the next load/device. Added a `keepActive` flag on `users/{uid}/trips/{code}` (set on manual un-archive, cleared on any archive) that auto-archive now skips → manual un-archive persists. (2) **Cross-device sync** — new read-only listener `subscribeUserTrips()` (`.on('value')` on `users/{uid}/trips`, attached in `loadUserTripsFromFirebase`) keeps `_archivedTripCodes`/`_keepActiveTripCodes`/`_accountTripCodes` live, so an archive/un-archive on one device reflects on the user's other devices WITHOUT a reload. (3) **Teal button** — the "+ New Trip" landing button used `var(--theme-accent)`, so it followed whatever trip theme was on the body (fire-island/adirondacks accents are teal); pinned to brand navy `#1a3260` via `.landing-ctas button:not(.secondary)`. FIP-safe: per-user flags + a read-only listener, no trip-data writes. node --check pass (manual-test cycle skipped at Robbie's request for speed).
- v129 — **Archive trips (per-user) — BACKLOG #22, design locked with Robbie.** A trip can be tucked into an "Archived" section on the home screen instead of cluttering the active list, without deleting anything. Stored as a boolean on `users/{uid}/trips/{code}.archived` — **per-user, NEVER touches trip data**, so it's reversible and FIP-safe. New globals `_archivedTripCodes` (populated in `loadUserTripsFromFirebase` from the account map) + `_showArchived`. `setTripArchived(code,archived,silent)` writes the flag via `.update`. `autoArchivePastTrips()` runs on load and archives any trip whose `endDate` is >1 day past (no `endDate` → never). `recordTripForUser` switched from `.set` to `.update` so re-opening a trip PRESERVES its archived flag (viewing never un-archives). `renderLanding` splits "Your Trips" (active) from a collapsible **"Archived (N)"** section; the manual Archive/Un-archive toggle lives in Trip Details → Archive (`toggleArchiveCurrentTrip`/`renderArchiveButton`, label set on drawer open). **Un-archive is manual-only** (no date-based un-archive). FIP rule = option (a), Robbie's choice: FIP (ended Jun 16) + MDW (ended May 25) auto-archive now; Juneteenth (Jun 19–21, upcoming) stays active. node --check pass, runRegressionTests 25/25 (guest, Robbie).
- v128 — **Fix: a trip missing from one device's cache now re-fetches itself + cleanup of `null` orphan trips.** Robbie's iPhone showed only 2 of 3 trips on the home screen (Juneteenth missing) plus a `null`-coded trip in App Admin → Data; desktop was fine. Cause: Juneteenth's CODE was still in the iPhone's device list but its DATA was missing from the local cache (a save that failed earlier while the iPhone was storage-full), and v124's `loadUserTripsFromFirebase` only re-fetched trips ABSENT from the device list — not ones present-but-data-missing — so it never re-pulled Juneteenth, and `renderLanding` filtered it out (no data to render). Fix: (1) `loadUserTripsFromFirebase` now re-fetches any account trip whose data isn't in `getAllTrips()` NOR `_accountTrips` (catches BOTH absent-from-list and data-missing-from-cache) → the trip re-caches from Firebase and reappears on its own. (2) `repairCorruptedTripCodes` now prunes orphaned/invalid keys from the local trip cache (literal `null`/`undefined`/empty keys or non-object values) so they stop showing in App Admin → Data. FIP-safe: the re-fetch is a Firebase READ; the prune is local-cache hygiene only (never touches Firebase, never removes a real trip). node --check pass, runRegressionTests 25/25 (guest, Robbie).
- v127 — **On-screen "Storage & Diagnostics" readout (no console needed — for phones).** Adds App Admin → Data → "Show storage & diagnostics" button + `window.showDiagReport()`, which renders into the page: version/build, **sign-in state** (logged-in account vs Guest), **trips cached on THIS device vs trips on your Firebase ACCOUNT** (`_accountTripCodes`) with ⚠ flags for device-only / account-only mismatches, and localStorage per-key sizes + total. Read-only — no writes, no Firebase, no state change → FIP-safe. Purpose: (1) check storage on a phone without a console; (2) diagnose why surfaces show different trip lists — expected cause is a surface in **Guest mode / a different login** (the account trip-list is empty without sign-in, so it shows only that device's local trips). node --check pass, regression 25/25 (guest, Robbie).
- v126 — **Stop the local backup from duplicating embedded photos — frees ~2.4 MB (Robbie's device was at the 5 MB cap).** `diagStorage()` (v125) on Robbie's phone showed localStorage AT the cap (5120 KB), ~95% from ONE trip: FIP 2026 (`fip-2026-0611-yw`) carries **128 base64 `data:image` photos ≈ 2.38 MB**, stored once in the live cache (`trips.v1`) and a SECOND full copy in the backup (`tripBackup.v1`) = ~4.76 MB of 5 MB. Photos come from `compressPhoto`→`toDataURL` (uploaded images become base64 inside the trip). Fix: the local **backup** is now image-light. New `_stripDataImagesForBackup(ev)` deep-clones the event and blanks any `data:image` string (keeps all dish/recipe structure); `saveLocalBackup` stores that clone; one-time `_migrateStripBackupImages()` (called once in `startAppAfterAuth` after `repairCorruptedTripCodes`) strips the EXISTING backup blob on load so the ~2.4 MB frees immediately. **Restore reworked to PREFER Firebase:** `restoreFromBackup` now fetches the live Firebase trip first and uses it if it has dishes (full, with photos), only falling back to the image-light local backup when Firebase is genuinely empty — so a restore can NEVER overwrite real photos (esp. FIP) with blanks. FIP-SAFE: strip runs on a CLONE; the live cache copy + Firebase record are untouched (FIP's photos still display + Firebase stays full); no trip/backup is deleted. The deeper root fix (convert the 128 base64 photos to hosted URLs / IndexedDB so the live copy + Firebase record also shrink) remains a careful follow-up (#33). node --check pass, runRegressionTests 25/25 (guest, Robbie). Expected after deploy: backup entry tiny, total ~2.7 MB.
- v125 — **Read-only storage diagnostic `window.diagStorage()` — measure before slimming the cache (#33).** Adds a console tool that reports total localStorage use vs the ~5 MB iOS cap, a per-key table (sorted biggest-first, with a count of heavy embedded `data:` images), and per-trip breakdowns for the live cache (`trips`) and the `backups` map. READS ONLY — writes nothing, no Firebase, no state mutation → cannot lose data; FIP-safe. Purpose: see WHERE the space actually goes (many cached trips vs. heavy embedded photos) before cutting, so v126 trims precisely. **Must run on the LIVE site on the actual full device** — localhost is a separate, near-empty origin. Two findings logged while scoping the next build: (1) `saveLocalBackup` keeps a FULL copy of every trip ever saved (Firebase already holds them) = the prime redundant local sink → v126 will CAP the backup to the ~3 most-recently-saved trips (the current trip always survives the cap; backup is a tertiary copy, data also lives in Firebase + the `trips` cache, so the cap can't remove the last copy of anything). (2) **Do NOT strip images from the backup** — `restoreFromBackup` → `commitAndSync` writes the restored trip back to Firebase, so a stripped backup could overwrite real images with blanks (data loss). node --check pass, runRegressionTests 25/25 (guest mode, Robbie).
- v124 — **Joined trips now show on a storage-full device (the flip side of v123).** Robbie's iPhone: trips he joined on another device didn't appear on the phone's home screen, but showed fine on desktop (same account). Root cause: `renderLanding` built the home list ENTIRELY from localStorage — `getDeviceTrips()` + `getAllTrips()`. The cross-device bridge `loadUserTripsFromFirebase` reads the account's joined trips from Firebase `users/{uid}/trips`, then tried to persist each one locally via `saveToLocal`/`recordDeviceTrip`; on a full device those writes are silently swallowed by v123's `_safeLSSet`, so the trip never landed in `getDeviceTrips()` and never rendered. Desktop has free storage → writes succeed → trip shows. Fix: the home list is now driven by the Firebase account trip-list held in memory, so local cache is an optimization, not the source of truth. New globals `_accountTrips` (code→event) + `_accountTripCodes`; `loadUserTripsFromFirebase` populates them from Firebase (and still tries to cache locally) then re-renders; `renderLanding` renders the UNION of device-cached trips and the in-memory account trips (prefers local copy, falls back to in-memory for uncached ones); `deleteTripFromDevice` also clears the in-memory entry so a removal sticks without reload. Guest mode unaffected (no `currentUser` → `_accountTripCodes` stays `[]`, device-local as before). No trip write-path change, `validateBeforeFirebaseWrite` untouched, nothing deleted → FIP-safe. Verified: `node --check` pass, `runRegressionTests()` 25/25 (Robbie). Does NOT free storage — that's the next build (v125, slim the local cache, BACKLOG #33).
- v123 — **iOS storage-full no longer blocks the cloud save (Robbie's iPhone QuotaExceededError).** Root cause: on iOS, localStorage (~5MB) fills with accumulated trips/images/backups; `setAllTrips`/`setDeviceTrips`/`setUserData` wrote raw with NO try/catch, so a full store threw `QuotaExceededError` — and because the save path is **local-first then cloud** (`saveState` then `fbWriteTrip` in autosave/commitAndSync/visibilitychange), the throw aborted the function BEFORE the Firebase write, skipping the sync entirely and surfacing the generic red "something went wrong" banner (global onerror). So iPhone edits were silently not saving (desktop's bigger quota hid it). Fix: new `_safeLSSet(key,value,label)` helper wraps the write in try/catch — on `QuotaExceededError` it warns, shows ONE calm toast ("storage full — changes still syncing to the cloud…"), and returns false **without throwing**; routed `setAllTrips`/`setDeviceTrips`/`setUserData` through it. Net: the local write can fail harmlessly and the Firebase write (the real source of truth) still runs every time. **NOTHING is deleted** (Robbie's explicit constraint) — no eviction, no trimming; a full device just stops updating its local cache and self-heals from Firebase on next load. No Firebase write-path change, validateBeforeFirebaseWrite untouched → FIP-safe. Verified on a throwaway trip: console showed local write skipped → fbWriteTrip ran with all dishes → write echoed back from Firebase (timestamp-guard match = it landed). runRegressionTests 25/25.
- v122 — **Adjusted-amount override + every view reads the adjusted (Robbie).** Bug: the dish/recipe **detail view** and the grocery **"Edit ingredient" popup** showed the ORIGINAL (base) quantity while the grocery list + cooking mode showed the scaled amount — and the editor's **"Adjusted" box was hard-locked (`readonly`)**, so Robbie couldn't apply his own judgement. Fix: new **additive** ingredient field `qtyOverride` (empty = auto-scale exactly as before; non-empty = the manual amount, shown verbatim everywhere with NO math). (1) **Dish-editor "Adjusted" box is now editable** — type your own → row flags + turns amber (`.is-override`), clear the box to revert; new `markIngredientOverride`; `updateScaledIngredientDisplay` skips overridden rows; `collectIngredients` persists it; `addIngredientRow` gains an `override` param; `editDish` + the library-load path seed it back in. (2) **`normalizeDish` carries `qtyOverride` through BOTH ingredient branches** so it survives the Firebase round-trip (the field would otherwise be stripped — it rebuilds each ingredient from an explicit field list). (3) **Read sites honour it:** `aggregateRows` (grocery total), `renderCookingIngredients`, and `renderGroupedIngredientsHtml` (detail) — the detail view now also shows the SCALED amount instead of the base (the reported bug), and overridden items get a ✎ badge. (4) **Grocery "Edit ingredient" popup** (`editDishIngredient`/`saveDishIngredientEdit`) now shows the ADJUSTED amount with a muted "Recipe base: X" hint, and saves your number as the override; the base `quantity` is left untouched (still editable in the full dish editor). Bonus: cooking mode now uses `scaleQty` so fractions like "1 1/2" scale correctly (was naive `parseFloat`, which dropped the fraction). Design = single source of truth on the meal-plan dish; **original `quantity` is never modified → FIP-safe**, override is opt-in (no backfill / no mass write). Verified by Robbie on a throwaway trip + `runRegressionTests()` 25/25.
- v121 — **Assign-stores grid overhaul (keyboard-fast + recipe pills + polish).** (1) **Keyboard nav** (laptop): the grid table is focusable; **↑/↓** move the highlighted row, **←/→** move store column, **1–9** set the row to that store (number shown in each column header) **and auto-advance to the next row** (rapid single-store entry), **Space/Enter** toggle the focused cell (Enter advances) for multi-store. New `handleAssignKey`/`_renderAssignFocus`/`_aFocusRow`/`assignRowToStore`/`_assignStores`/`_aFocus`. (2) **Clickable recipe pills** (Robbie): each item shows pills for the dish(es) it's for (from `row.usedFor`, deduped by dishId); clicking a pill opens that dish to edit via the existing `editDish(id)`. (3) **Polish:** assigned cells get an accent fill (scan the pattern at a glance), unassigned rows get an amber left edge + an "N left to assign" count, numbered store headers, focus highlight, cleaner item/qty layout. setItemStore extended to keep the same visuals on mouse clicks. NOT runtime-verified (keyboard flow needs a laptop). No save-path change — each assignment still writes via the existing setItemStore→saveGroceryStateNow (self-echoes are blocked by the sync timestamp guard, so rapid keying won't re-render/lose focus). FIP-safe (store-assignment writes only; existing path). NOTE: this is the manual/override grid — the planned **per-trip AI store assignment** (type the trip's logic in plain English → AI sorts; then DELETE the wrong built-in auto-guess) is still a separate future build (see BACKLOG).
- v120 — **FIX: image sourcing now reliably pulls the recipe URL's photo first (#25).** `sourceRecipeImage` step 1 (the recipe URL's own image via Microlink) was the ONLY one of the three Microlink calls missing `&force=true` (URL import line 6357 and backfill line 2141 both had it). Without force, Microlink returned an empty cached result, so URL-first quietly failed and the function fell through to Spoonacular → Pexels (generic image) even when the recipe had a perfectly good page photo. Added `&force=true` + aligned the timeout to 9s so the URL image wins as intended. Image-fetch only, no data/sync. (Not runtime-verifiable from here; verify a recipe with a URL now shows its page photo, not a Pexels stock one.)
- v119 — **shop: "Uncheck all" moved to the top** (Robbie), paired with ✕ Done on a flex row (Done left, Uncheck all right). Removed the bottom placement. Render-only.
- v118 — **shop-mode redesign — one focused screen, no duplicate controls (#27).** Robbie flagged the clunk: the List toolbar was never hidden in Shop, so shoppers saw TWO resets (filter-reset vs checkoff-reset), a store dropdown AND store chips, and "Needed" AND "Hide done". Fix: hide the entire List toolbar (`.grocery-workspace-controls` + mobile filter handle) while shopping, and give Shop its own single toolbar — **✕ Done** (exits to List, where the List/Assign/Shop tabs live), a **search-or-add box** (filters live, sharing the List `grocerySearch` state = one source of truth; no match → "+ Add 'x'" which creates a manual item via the SAME path as the List "+", so data flow stays consistent), **store chips**, one **slim progress + Hide done**, and **↩ Uncheck all** tucked at the bottom. Bought items still sink to the bottom of their OWN store section (no separate "done" bucket — Robbie's call). buildGroceryList routes the empty/no-match case through renderShoppingList so the search box survives. Search focus+caret preserved across rebuilds. Net: every control appears once. Render + reuse of the existing manual-add path; no new sync/data risk, FIP 2026 untouched.
- v117 — **shopping split-by-store filter (#27 multi-shopper, safe bit 1 — pure render, ZERO writes).** A row of store chips at the top of shopping mode ("All" + each store, each with a remaining-count) lets a shopper filter to just the store they're at (divide & conquer); "All" is always the first chip so the full list is one tap back. New display-only state `_shopStoreFilter` + setShopStore (just re-renders, no Firebase). Chips use `data-store` + attached click listeners (NOT inline onclick) so store names with apostrophes like "Trader Joe's" don't break. Only shows when >1 store. No data/sync/groceryMeta touched — FIP 2026 safe. NEXT safe bit: "who's shopping now" (reads presence). THEN the sensitive per-item write (branch + test trip + 2-device proof).
- v116 — **shopping filter clarity (one toggle per view).** Robbie noticed "Needed only" (list view: hides Bought + Have) and v115's "Hide done" (shopping: hides Bought) overlapped, and "Needed only" was silently applying in shopping mode even though its button is hidden there. Fix: `buildGroceryList` now applies the `groceryNeededOnly` filter only when NOT in shopping mode (`if(groceryNeededOnly&&!shoppingMode)`), so the shopping view is governed solely by its own "Hide done" toggle. "Have" items stay visible while shopping (Robbie's call). getCurrentGroceryRows (export/badges helper) left unchanged. One-line render-path change, no data/sync.
- v115 — **shopping-mode declutter (#27, phase 1 — render/CSS only).** (1) **Cleaner in-aisle row** — dropped the dish-provenance ("· Chicken Tacos, Fajitas") from each shopping row; name + qty stay prominent, department stays as the faint right tag. (2) **"Hide done" toggle** (new `_shopHideBought` + toggleShopHideBought) — collapses checked items so the list shrinks as you shop; empty/all-bought sections drop out. (3) **Store-complete moment** — a fully-checked store gets a green ✓ header and auto-collapses. (4) Progress now leads with "**N left**". No data/sync touched. NEXT (phase 2, sensitive): multi-shopper — per-item Firebase writes so concurrent check-offs don't clobber (saveGroceryStateNow currently does a whole-trip .set), "who's shopping now", and the split-by-store per-shopper filter (Robbie: they sometimes split, sometimes shop together).
- v114 — **C0h remove the cooking-mode people adjuster + pill ordering.** (1) **C0h** — removed the "For [N] people" input from the cooking header (it didn't actually work; serving scaling belongs on the dish). renderCookingIngredients now scales to the dish's own `peopleEating||servings` instead of the removed `#cookingFor` input; cleaned the three dead `cookingFor` setters (cookRecipe/switchCookingDish/resumeCookingDish). The dish-edit form's own "Cooking for" (peopleEating / #cookingForField) is untouched. (2) **Pill ordering** — the dish-switcher pills now shift dishes with a live timer to the **left, soonest-first** (renderDishSwitcher sorts a copy; `data-ptimer` keeps each pill's original index so `_updatePillTimers` still targets the right badge). Reorder happens only on start/stop/switch/fire events, not every second, so pills don't constantly reshuffle. Render/CSS only, no data paths.
- v113 — **cooking-mode quick wins (C0d/C0f/C0g + pill seconds).** (1) **Pill seconds** — the dish-switcher pill badge now shows full mm:ss (renderDishSwitcher) and updates live every second via `_updatePillTimers()` from the global tick (was "30m" and stale). (2) **C0g status-bar overlap** — the overview screen is `position:absolute; inset:0`, so it ignored `.cooking-mode`'s safe-area padding and sat under the clock; gave `.cooking-overview` its own `padding-top: calc(24px + env(safe-area-inset-top))`. (3) **C0d dish image not emoji** — showCookingOverview now shows the dish's real photo (`_displayImageUrl`) in a 56px rounded thumb, falling back to a clean accent gradient (`.cooking-overview-thumb--blank`), not `getRecipeEmoji`. (4) **C0f Back on the overview** — added a "← Back" button to the pre-cook overview (you were stuck there until Start if you opened it by accident); it calls `exitCookingMode(true)` — new `skipRating` param so backing out of the overview doesn't pop the rating modal for a dish you never cooked. Render/CSS only, no data paths touched. NOT runtime-verified (live test by Robbie).
- v112 — **C0a cooking timers: persist + run in parallel** (cooking-mode redesign, part 1). Root bug: exitCookingMode wiped _cookDishState and killed the engine, so leaving cooking mode deleted every timer. Now: (a) **wall-clock model** — each dish timer stores `endsAt` and remaining is computed from the real clock, so it stays correct through phone sleep / tab throttling; (b) timers **survive leaving cooking mode** (Back no longer wipes state or stops the global engine while timers run); (c) **persistent bottom strip** (`#timerStrip` / renderTimerStrip) visible app-wide while any timer runs — one chip per timer (dish name + mm:ss), tap-to-resume that dish, ✕ to cancel one, "Clear all" for 2+; (d) **fired alert** = beep (WebAudio, unlocked on the start tap) + haptic + a persistent red "DONE" chip until acknowledged; (e) **exit vs end split** — Back leaves timers running, explicit `endCookingSession()` clears all. One timer per dish (Robbie's call); strip hidden while the fullscreen cooking UI is open (the in-cook timer + dish pills already show it). Also fixed `switchCookingDish` (was force-saving a running timer as stopped) and `_saveCookDishState` (now merges so endsAt/fired survive). LOCAL UI STATE ONLY — no Firebase/sync/save/grocery touched. Lock-screen live timer is iOS-native-only (ActivityKit) — parked on the native track #21. NOT runtime-verified (tested live by Robbie post-deploy + runRegressionTests 25/25).
- v111 — image fallback switched to PEXELS (Google Custom Search is deprecating / "search entire web" decommissioned). Order: recipe URL → Spoonacular → Google(if keys) → Pexels. _pexelsImage (pexels.com/v1/search, Authorization header). Pexels key syncs with trip. Settings: one Pexels key field (replaced the two Google fields). VERIFIED working by Robbie.
- v110 — Google image fallback (later superseded by Pexels v111) + image keys (Spoonacular/Google/Pexels) now STICK WITH THE TRIP (#29): saveImageApiKey/saveGoogleImageKeys/savePexelsKey write currentEvent.shared* + commitAndSync; loadSharedApiKey pulls them down; getters fall back to the trip.
- v109 — #26 edit a dish's item from the grocery row's dish pill: aggregateRows carries dishId+ingredient index; pills tappable → pickDishIngredient (picker if a dish feeds the row via >1 ingredient; no ids shown) → editDishIngredient modal (qty/unit/item/grocery name) → saves to dish + re-flows.
- v108 — #25 smarter image search: _simplifyDishQuery reduces a title to its core dish ("Peach, Arugula & Burrata Salad" → "arugula burrata salad"); sourceRecipeImage tries full title then simplified on Spoonacular. URL still first.
- v107 — teammates' profile photos now show when online: writePresence re-reads getUser() each tick (was captured once at startPresence), + saveProfile re-pushes presence. Mid-session photo/name changes propagate.
- v106 — deleted trips stop reappearing: deleteTripFromDevice now also removes users/{uid}/trips/{code} so loadUserTripsFromFirebase (load / pull-refresh) can't re-add it. Trip data untouched.
- v105 — #27B robust AI grocery categorization: cleanGroceryNames returns {grocery name, dept} in one call (dept from GROCERY_DEPTS incl. Spices & Seasonings / Condiments & Oils); groceryDept stored; list + detail sections use groceryDeptFor (AI dept else improved guessDept — spices before produce, black pepper/salt → Spices, bell/jalapeno → Produce). Kills the salt/pepper class.
- v104 — #23 cook time end-to-end: showDishDetail shows ⏱; addDishToTripInBackground + saveDishFromModal carry/preserve cookTime.
- v103 — #24 Paste Full Recipe now pulls cooking steps (schema gains steps, stops excluding instructions, sanitizeParsedRecipe preserves them, applyParsedRecipe → populateSteps).
- v102 — A2 AI chat input clears after sending a normal message (only the URL/build-recipe paths cleared it before).
- v101 — grocery section categorizer fix (red wine vinegar no longer Alcohol; condiments/oils before alcohol; cheeses → Dairy).
- v100 — edit grocery names: tap any ingredient in a recipe or dish detail view to set/override its grocery-list name (editGroceryName via promptAction, prefilled with the current name). Recipe detail edits the library recipe (targeted menuLibrary/{id}/ingredients write); dish detail edits the plan dish (commitAndSync). Rebuilds the grocery list + re-renders the detail. A hint line + pill tooltip indicate it's tappable. renderGroupedIngredientsHtml now takes a ctx {type,id}.
- v99 — grocery names work FRONT-TO-BACK. Root insight: groceryName lives on ingredients, which exist in TWO copies — library recipes AND the trip's dishes (copied on add-to-plan). The grocery list + meal plan read from currentEvent.dishes, so cleaning only the library (v96) never reached them → no pills on the meal plan. Now: (a) _cleanTripDishes() cleans the OPEN trip's dishes + commitAndSync; (b) SELF-HEALING _autoHealGroceryNames() runs from buildGroceryList — first time a trip's grocery list builds, it auto-fills any missing names (once/trip/session, re-entry guarded, transparent "Tidying grocery names…" toast), then renderAll so the plan pills appear; (c) the backfill button now cleans library AND the open trip; (d) adding a cleaned recipe to a plan already inherits groceryName (normalizeDish preserves it). So regardless of how an ingredient arrives (URL/photo/paste/manual), it gets a grocery name and the pill shows everywhere + the list merges by it. Shared helpers: _cleanIngredientNames, _ingNeedsGroceryName, _tripHasMissingGroceryNames. NOTE: pill only shows when the grocery name differs from the item (by design). Auto-heal uses the Anthropic key (bounded to the open trip's dishes, once/session).
- v98 — FIX: backfills said "open the Recipe Library first" when run from App Admin (can't be in both). New _ensureLibraryLoaded() one-shot fetches menuLibrary on demand (no-op if a live subscription already populated it); both backfillGroceryNames + backfillMissingImages now load the library themselves and show "Loading your recipes…". So "Clean grocery names (AI)" / "Backfill missing images" just work from anywhere.
- v97 — FIX: "Clean grocery names" crashed when run with no trip open. backfillGroceryNames called buildGroceryList → aggregateRows did currentEvent.dishes.forEach on a null currentEvent. Fix: aggregateRows returns [] if !currentEvent (defensive root fix); backfillGroceryNames + the at-import trigger only call buildGroceryList when a trip is open. PLUS (Robbie's request): both backfills now show TOAST notifications instead of console-only — start ("Cleaning grocery names for N…" / "Sourcing images for N…"), done ("✓ Cleaned N recipes, M more — run again" / "✓ Added N images (M had no online match)"), and clear messages for no-key / library-not-open.
- v96 — grocery simplification Stage 2b: AI grocery-name cleaning. cleanGroceryNames(items) = one batched Haiku call that simplifies each ingredient to its shopping-list name (strips qty/package/brand/prep words; keeps real distinctions like "crushed tomatoes"; NEVER invents/adds/merges). _applyGroceryNames(recipe) sets ingredient.groceryName for ones missing it and persists a TARGETED write to menuLibrary/{id}/ingredients (guarded: only when there are ingredients + actual changes; preserves all ingredient fields). Runs (a) at import (confirmRecipeImport, async non-blocking) and (b) via window.backfillGroceryNames() / the new Settings button "Clean grocery names (AI)" (15 recipes/run, paced 1s). The cleaned name flows into the v95 pill + the v94 grocery-list merge. NEXT v97: editable "Grocery name" field in the dish edit form for manual overrides.
- v95 — grocery simplification Stage 2a: show BOTH names. normalizeDish now PRESERVES a per-ingredient groceryName (it previously rebuilt ingredients with only known fields, so groceryName would've been dropped on every load — this is the must-fix foundation). Inline 🛒 pill (.grocery-name-pill) shows the grocery-list name next to the original ingredient in the detail views (renderGroupedIngredientsHtml → recipe + dish detail) and the plan card's collapsed ingredient list — only when it differs from the item, so no clutter. Uses groceryDisplayName (heuristic now; AI fills groceryName in v96). Render-only + one additive normalize field; stored ingredients untouched. NEXT v96: AI cleaning at import + window.backfillGroceryNames() tool. v97: editable Grocery name field in the dish edit form.
- v94 — GROCERY SIMPLIFICATION Stage 1 (#10/#2, option A exact-unit, render-only, NO writes to dish data). (a) groceryDisplayName(ing) = stored groceryName if present (Stage 2 AI later) else heuristic (text before first comma, drop trailing parenthetical); aggregateRows now merges on this cleaned name so "crushed tomatoes, drained" merges with "crushed tomatoes". (b) _groceryNameNorm() is now the SINGLE name normalization used by BOTH the merge key (legacyNormKey) and the duplicate-flag banner — they previously used different plural rules, which is why the banner flagged dups the merger wouldn't merge (#10 false flags + split qty). The stored dish ingredients are NEVER modified — this only changes how the grocery list groups/displays. Grocery meta (store assignments) re-keys to cleaned names (Robbie: not using grocery state now, acceptable). NEXT: run diagGroceryItem('orange juice') to confirm the quantity-split root cause for Stage 3; Stage 2 = AI groceryName at import.
- v93 — MERGED the overnight-data-risk branch items to live (reviewed; 25/25 regression confirmed on v92 first). Cherry-picked: #1 menuLibrary echo guard (subscribeToMenuLibrary suppresses _recentlyDeletedRecipeIds for 30s + deleteFromMenuLibrary removes locally — deleted recipes stop reappearing); #3 window.cleanupDuplicateRecipes() (dry-run console tool; deletes only on {apply:true}); #6 cook photo stamped at assign time (doAssignDish uses resolvePersonProfile so assignedToPhoto travels); #10 window.diagGroceryItem() (read-only grocery diagnostic — prerequisite for the grocery quantity fix). #1 touches the sync listener and #6 the save path (both additive); re-run runRegressionTests() to confirm 25/25 on v93.
- v92 — auto-source recipe images (#5, Robbie's spec). New sourceRecipeImage(recipe,opts): (1) the recipe URL's OWN photo via Microlink (always preferred), (2) else a similar dish via Spoonacular complexSearch (needs image API key), then PERSISTS to menuLibrary/{id}/imageUrl (targeted write, like v75) — guarded to library recipes only (fromLibrary / in _menuLibrary) so it never writes a bogus entry for a plan-dish id. Triggers (each only when no real image): opening a library recipe detail; saving a bank recipe (force-updates when a URL was just added → "URL added later updates image"); confirming an import (fills photo-imported recipes that now have no image). window.backfillMissingImages() console tool fills up to 25 image-less library recipes, paced 1.2s. New Settings card "Recipe Image Key (Spoonacular)" — getImageApiKey/saveImageApiKey/clearImageApiKey (local-only key; found images persist so all devices see them) + a Backfill button; syncAdminInputs populates it. _imageFetchAttempted dedups per session. NEEDS a Spoonacular key (free, spoonacular.com/food-api) for the online-search fallback; URL-photo path works without it. NOT runtime-verified (can't test Spoonacular/CORS/vision from here).
- v91 — FIX wrong recipe images (Robbie): uploaded screenshots must NOT be the recipe image. (a) photo/AI import no longer stores the screenshot as recipe.imageUrl (removed the line in _extractRecipeFromImages); (b) replaced v90's _fallbackRecipeImage (library-borrow, which spread one screenshot to 4 recipes) with _displayImageUrl(recipe) = show imageUrl ONLY if it's a real http(s) URL — this also DROPS already-stored data: screenshot images at render time (no Firebase write) so the 4 wrong images disappear; (c) no more borrowing another recipe's photo. Wired into renderRecipeCard + both detail heroes (and the dish→library resolution). STILL TODO (asked Robbie): online image sourcing — recipe URL first (Microlink already does this for URL imports), then a similar dish online for photo-imported recipes that have no URL. Render-only, no writes.
- v90 — recipe image fallback (#5, render-time) + finish v89 button on mobile. _fallbackRecipeImage(recipe): when a recipe has no image of its own (and, for plan dishes, no linked-library image), borrow one from another library recipe whose name shares a significant (>3 char) word — wired into renderRecipeCard, showRecipeDetail hero, showDishDetail hero (last resort). RENDER-TIME ONLY — never writes to Firebase, so nothing wrong persists and it's trivially reversible. ALSO: #aiPhotoBtn gets the 36px mobile treatment so the AI chat 📷/✶/↑ buttons don't crowd. CAVEATS (eyeball these): borrowed images can look slightly off (showing a similar recipe's photo); plan dish CARDS still don't show image thumbnails (adding them is a layout decision — deferred); true "source from URL then persist" backfill is window.backfillRecipeImages (v75) + needs the fallback-source decision for a persistent version. NOT runtime-verified.
- v89 — AI assistant photo import (#4): 📷 button + hidden multi-file input in the AI chat input row → aiChatImportFromPhotos → reuses the v87 vision core (_extractRecipeFromImages) → standard import review panel (Save → library, v81 dedup). Posts chat status messages; same strict no-fabrication contract. Additive, client-side; save path is the existing reviewed one. NOT runtime-verified.
- v88 — pull-to-refresh on the home/landing screen no longer errors "No trip open" (#1). The global pull handler (setupPullToRefresh touchend) now branches: if a trip is open → refreshCurrentTrip; else (landing) → loadUserTripsFromFirebase + renderLanding + "Refreshed." toast. Render/data-read only, no writes.
- v87 — Photo import overhaul + mobile import-card fit. (a) MOBILE FIT (#3): entry-tabs wrap (flex-wrap) + 2×2 on ≤560px, shorter labels (Paste Recipe / Ingredients), removed duplicate 'active' on pasteRecipePanel so only one panel shows on open. (b) PHOTO IMPORT: dropped capture="environment" so iPhone shows the Photo Library (pick screenshots) + added multiple; new drop/paste zone — paste a screenshot (Ctrl/⌘+V, scoped so it never hijacks text paste) or drag images in; MULTI-IMAGE staging with thumbnails + remove + "Extract recipe from photos" so a recipe spanning several screenshots is combined in ONE Claude vision call. Refactored into shared _extractRecipeFromImages(dataUrls) (reused by the AI bot in v89); importFromImage kept as a back-compat single-file wrapper; first image → recipe.imageUrl. Same strict no-fabrication contract. Additive, client-side only, no Firebase/data changes. NOT runtime-verified (paste/camera/album behaviour needs real devices).
- v86 — recipe library GROUP-BY toggle (#16): All / By chef / Recently added, rendered at the top of the library overlay (renderMenuLibraryOverlay). Owner groups by r.owner||r.addedBy; date groups by savedAt/addedAt month, most-recent first, with formatted "Month YYYY" headers + counts. New _recipeGroupBy state + setRecipeGroupBy(). Render-only, no data writes. NOTE while here: #7 (recipe name wrap in view) and #8 (AI Build vs Send button overlap on mobile) were ALREADY fixed in v72 (index.html lines ~1399 and ~1403); #14/#15 (cook avatar on plan card + detail) already done in v71. Verified present, not re-done. "Category" grouping skipped — recipes have no category field (would require inventing one); offered the two groupings that map to real fields. NOT runtime-verified.
- v85 — IMPORT FROM PHOTO (new) + import transparency. New "📷 Photo" tab in the dish-modal import card: camera/file → _downscaleImage (canvas, max 900px, JPEG q.72) → Claude vision (claude-haiku-4-5, base64 image block) with the SAME strict no-fabrication contract as URL import ({"error"} if no recipe, extract only what's visible) → existing showRecipeReviewPanel → confirmRecipeImport (v81 dedup). Downscaled photo stored as recipe.imageUrl (fixes "paste has no image"). switchEntryMode extended with 'photo'. Reuses stripJsonFences/getApiKey/showRecipeReviewPanel/makeId/getTodayStr. Transparency: URL-import no-recipe failures now say "nothing was imported (no recipe invented)" + point to Paste/Photo; success toast names source ("Extracted from the page at <host>"). Additive, no Firebase/sync changes. NOT runtime-verified (can't test vision extraction from here).
- v84 — meals render in canonical order (#9/#13) — orderedDayMeals() sorts a day's meals by trip mealTypes then canonical Breakfast→Lunch→Dinner→Snack→Drinks→Dessert; returns a sorted COPY, never mutates day.meals (display-only). Fixes "re-added meal jumps to bottom".
- v83 — ingredient sort toggle (Section / Recipe order / A–Z) at top of detail-view ingredients (setIngSort re-renders open detail; showRecipeDetail now nulls _dishDetailId) + cooking blank fix (getCurrentCookingRecipe uses _findRecipeForCook so a plan dish resolves instead of rendering blank). Render/lookup only.
- v82 — pre-trip safe build (render-only, no data writes). P0: meal-level Cook button now appears for _menuLibrary recipes — renderMealBlock hasCookable scans recipeBank ∪ _menuLibrary (matching what cookMeal already resolves from) and also counts a dish's own steps (fixes C2 meal-level bug). P1: (a) removed notes/expandable body + "tap for notes" from plan dish cards (notes still reachable via showDishDetail); (b) recipe + dish detail views group ingredients by grocery department (shared renderGroupedIngredientsHtml using guessDept; single-group recipes render flat); (c) strip baked-in "Step N:"/"N." prefixes from steps in both detail views (cleanStepText, delimiter-anchored so "2 eggs, beaten" is safe). NOTE: the briefed "VEG pill on library cards" was already done in v72 — the 🌿 the brief pointed at is in dead renderRecipeCardLocal_unused; skipped. GROCERY SIMPLIFICATION (P2) DEFERRED to post-trip — see #10 note (changing the name-derived grocery key would orphan groceryMeta and reconcileGroceryMeta would delete live store assignments).
- v81 — duplicate-upload PREVENTION (#3, prevention half). New shared findExistingLibraryRecipe(recipe,excludeId) helper (match = same normalized URL OR same name+source; conservative; never deletes), applied to EVERY menuLibrary create-path: saveDishToLibrary (upgraded from name-only), saveImportedToLibrary, executeAddBankRecipe (AI add), confirmRecipeImport (URL import), addDishToTripInBackground (reuses existing lib id so the trip dish points at it), and saveDishFromModal bankOnly (NEW saves only — editing path untouched). On a match the path reuses the existing recipe and shows "already in your library" instead of writing a second entry. Prevention only — no existing data read/changed/deleted. CLEANUP of EXISTING duplicates still pending (Robbie chose prevention-only this build).
- v80 — URL import hardening. (C7) shared stripJsonFences() helper applied to the two AI-parse sites that lacked it: <actions> parser and <recipe_draft> parser (URL-import parser already stripped fences). (C9) paywall handling in backgroundImportAndReview: known hard-paywall hosts (cooking.nytimes.com, nytimes.com) stop up front with "This page requires a subscription — copy the recipe text and paste it here instead."; and if Claude finds no recipe AND the page text has paywall markers, the same subscription message shows instead of the generic "no recipe." (C8) confirmed: v79's empty-page guard + no-prior-knowledge prompt already block pure fabrication; the apparent "fabrication" was paywalled pages returning >200 chars of teaser/login text — now caught by C9. No Firebase/sync/save logic touched (all changes are in the fetch/parse path before any write).
- v79 — URL import extracts from REAL page content via Jina Reader (r.jina.ai); prompt forbids prior knowledge/invention; stops (nothing saved) if page unreadable or no recipe. Verified working (correct recipe pulled).
- v78 — stop URL import when no page title fetched (no fabrication from empty content)
- v77 — Microlink cache-bust (&force=true) + confirm-the-page step before parsing
- v76 — cook dropdown/assign picker include presence users via getAssignableUsers() (newly-joined teammates show when online); interim for the user-directory need (#19)
- v75 — image backfill tool window.backfillRecipeImages() (console-triggered, targeted menuLibrary/{id}/imageUrl writes only, rate-limited); verified URL-import dedup (#7) intact
- v74 — AI assistant polish: fixed unstyled recipe-builder/url-import messages (ai-msg-user/ai-msg-ai had no CSS), flatter header/cleaner bubbles/input; AI fab lifts above dish modal Save footer
- v73 — AI chat fab+panel z-index raised (9150/9151) so the assistant is reachable over modals (dish edit, recipe detail, library)
- v72 — quick wins: recipe detail name wraps; AI "Build Recipe" icon-only on mobile (no send-button overlap); VEG pill on library cards (no more 🌿 emoji); network-aware URL import error message
- v71 — cook avatar photos via live resolution (resolvePersonProfile: getUser → presence → known users → initials); no data writes/bloat
- v70 — ingredient column headers Original/Adjusted/Unit; dish detail view shows imported recipe photo (resolved via bankRecipeId → name/url); editDish preserves bankRecipeId so image/link survives edits
- v69 — dish edit form: modal max-height + sticky footer so Save is always reachable on mobile; "Recipe serves" label; ingredient column headers; vegetarian → green pill toggle (:has)
- v68 — compact 2-line ingredient rows on mobile (qty/cooks-for/unit on row 1, name row 2; thin separators so many fit); safe-area header; 15px inputs; pure CSS, collectIngredients untouched
- v67 — dish edit form mobile compaction (modal padding, hide intro, Day/Meal side-by-side, smaller labels/cards)
- v66 — unified Owner/Cook for plan dishes onto assignedTo (label dynamic: "Cook" plan / "Owner" library); kept owner for library attribution; back-compat fallbacks
- v65 — dish view: hide Cook button when no steps; always show cook status (avatar+name or "No cook assigned"); events get no cook line
- v64 — dish detail VIEW trial (showDishDetail): tap a plan dish → read-only view reusing recipe-detail format; Edit → existing form; backup branch backup-v63-pre-dish-view created
- v63 — more meal-level depth (stronger shadow, bigger gap); event dish names render in theme accent color (pill removed)
- v62 — meal-level depth: meal blocks raised cards + more gap; dishes flat bordered rows inside
- v61 — event distinction via "Event" pill (later replaced by accent text in v63); meal labels bigger uppercase
- v60 — plan tab de-clutter: removed meal-type icons + event emoji; event dishes toned down; VEG pill on meta row; dropped redundant "serves" when "For" shown; unified meal-head backgrounds
- v59 — trimmed iPhone footer bottom gap (mobile additive 20px→8px)
- v58 — FOOTER FIXED (real root cause): footer padding-bottom:max(…calc(env())…) was resolving to 0px in Chrome, so buttons sat flush at screen edge. v52–v57 all reused this same broken expression → no change ever. Replaced with plain literal padding:12px 16px 20px + simple calc for mobile safe-area. Diagnosed via live getComputedStyle measurements.
- v57 — grid-template-rows 1fr→minmax(0,1fr) (correct but not the root cause); removed v56 position:fixed footer band-aid; mobile overlay 100dvh
- v56 — removed images.weserv.nl proxy (was causing intermittent image loss), direct imageUrl load with accent-stripe fallback (never emoji); removed emoji from recipe cards entirely; reverted unrequested v54 library-top padding
- v55 — CSS Grid layout for recipe detail footer (bulletproof), images.weserv.nl proxy for recipe images, veg filter now works in library overlay, owner filter dropdown added to library overlay, setRecipeFilter calls renderMenuLibraryOverlay
- v54 — flex:1+min-height:0 panel layout, safe-area via overlay padding, addBankToPlanner routes to trip picker when no days, null trip code/name filtered in showAddToTripPicker
- v53 — owner dropdown always shows current user, clearDishModal loads known users on open
- v52 — recipe detail Library Info section (who added, date, trips used), footer always shows +Plan/Edit/Cook buttons, desktop modal top breathing room
- v51 — recipe card image thumbnails, desktop centered modals, veg badge, paywall note, isPaywalledSource helper
- v50 — recipe detail full-screen overlay, view/edit modes, hero image, ingredient/step layout, Add to Plan flow
- v49 — view/edit mode toggle for planner, mobile density improvements, visual polish
- v48 — removed redundant syncFromFirebase from subscribeToCurrentTrip, addDishToTripInBackground writes full trip, checkLibraryHealth() and runSmokeTests()
- v47 — addBankToPlanner closes library overlay before opening dish modal, URL import deduplication by URL
- v46 — addDishToTripInBackground uses TripStore.setTrip()
- v45 — subscribeToCurrentTrip and refreshCurrentTrip use TripStore.setTrip(), dead code removed, grocery search debounced
- v44 — _isLoadingTrip flag, autosave updatedAt guard, mpLog on timestamp guard blocks, window.checkSyncFreshness()
- v43 — removeMealFromDay crash fixed, duplicate trip title removed, dark mode button removed, Add a dish text removed, blank trip grocery warning suppressed, guessDept adds Beverages category
- v42 — blank trip grocery warning suppressed, guessDept Beverages routing
- v41 — version mismatch banner added
- v40 — background Firebase refresh skips renderAll when not newer
- v39 — _isApplyingRemoteChange flag stops ping-pong sync loop
- v38 — syncFromFirebase migrated to TripStore.setTrip
- v37 — restoreFromBackup migrated, sync validation guards added
- v36 — createTrip migrated to TripStore, Firebase callbacks logged
- v35 — TripStore wrapper Phase 1A, openLoadedTrip and goToLanding migrated
- v34 — Phase 0.5 guardrails, mpLog, validateBeforeFirebaseWrite, runRegressionTests, checkVersionEnforcement
- v33 — repairCorruptedTripCodes, renderLanding null key filter
- v32 — openLoadedTrip currentCode assignment fixed

---

## DONE

- **Session v80→v101 (2026-06-04/05)** — URL-import hardening + paywall (C7/C8/C9), import-from-photo (album/multi-shot/paste/drop) + AI-bot photo import, dup PREVENTION (#3a) + dup-cleanup tool (#3b live), recipe-image fix (no screenshots) + auto-sourcing (Spoonacular) + image fallback, echo guard (#1), cooking blank fix, meal order (#9/#13), pull-refresh fix, library group-by (#16), cook-photo-at-assign (#6), and **grocery simplification (#10/#2) front-to-back** (AI groceryName, 🛒 pill everywhere, self-heal, tap-to-edit, fixed section categorizer). 25/25 regression confirmed on v101. See version history above for per-version detail. ✅

- Phase 0.5 complete — mpLog, validateBeforeFirebaseWrite, runRegressionTests, checkVersionEnforcement, window.mpDiag ✅
- Phase 1A complete — TripStore wrapper, openLoadedTrip and goToLanding migrated ✅
- Phase 1B complete — createTrip migrated, Firebase callbacks logged ✅
- Phase 1C complete — restoreFromBackup migrated, syncFromFirebase and subscribeToCurrentTrip validation guards ✅
- Phase 1D complete — syncFromFirebase migrated to TripStore.setTrip ✅
- Phase 2A complete — _isApplyingRemoteChange flag, ping-pong loop broken ✅
- Phase 2B complete — background Firebase refresh timestamp check ✅
- v44 sync fix — _isLoadingTrip flag, autosave guard, timestamp block logging ✅
- v45 dead code cleanup, TripStore consistency ✅
- v47 Add to Plan overlay bug fixed, URL dedup added ✅
- removeMealFromDay crash fixed ✅
- Duplicate trip title removed from plan tab ✅
- Dark mode button removed ✅
- Add a dish text removed from empty meal slots ✅
- Blank trip grocery warning suppressed ✅
- guessDept routes beverages to Beverages department ✅
- Version mismatch banner added ✅
- v56 recipe images: removed flaky proxy, direct load, emoji fallback eliminated ✅
- v56 emoji removed from recipe cards (clean accent stripe for no-image) ✅
- v56 reverted unrequested library-top mobile shift ✅
- v58/v59 recipe detail footer cutoff — root cause was max()/env() padding-bottom resolving to 0px; fixed with literal padding ✅
- v60–v63 plan tab de-clutter + meal-level depth (emojis removed, event accent text, VEG pill, unified backgrounds, raised meal cards) ✅
- v64–v65 dish detail VIEW (view/edit split for plan dishes) ✅
- v66 Owner/Cook unified onto assignedTo for plan dishes (owner kept for library) ✅
- v67–v69 dish edit form mobile compaction + scroll-to-Save fix (sticky footer) + veg pill toggle ✅
- v68 compact 2-line ingredient editor on mobile (Original/Adjusted/Unit + name) ✅
- v70 dish view shows imported recipe photo (survives edits via preserved bankRecipeId) ✅
- v71 cook avatar photos (live resolution, self + online teammates) ✅
- v72 quick wins (name wrap, AI/send button, VEG pill on library cards, import error wording) ✅
- v73 AI chat reachable over modals (z-index) ✅
- v74 AI assistant polish + fixed unstyled message bubbles ✅
- v75 recipe image backfill (all linked library recipes filled with photos) ✅
- #7 URL-import duplicate guard verified intact ✅ (NOTE: insufficient — see #3, dup uploads still occur)
- v76 cook dropdown includes presence users (interim) ✅
- v77 Microlink cache-bust + confirm-page step ✅
- v78 stop import when page unfetchable (no fabrication) ✅
- v79 URL import reads REAL page content via Jina Reader, no fabrication — VERIFIED working ✅

---

## OVERNIGHT AUTONOMOUS RUN (2026-06-04) — SUMMARY

Ran the agreed list (everything EXCEPT #18 App Admin restructure and #21 iOS Share Extension) unattended. Safe render-only work pushed to **live `main`**; data/sync/delete work committed to branch **`overnight-data-risk`** (NOT deployed — review before merge). Every build: `node --check` + static checks (version/tag/title agree, no NEW duplicate functions, guardrail flags present). **Browser `runRegressionTests()` 25/25, phone testing, and vision-extraction quality were NOT verifiable from here** — live builds are "landed, syntax-clean, not human-verified."

**LIVE (main, deployed) — test on phone:**
- v83 ingredient sort toggle (Section/Recipe/A–Z) + cooking blank fix (getCurrentCookingRecipe→_findRecipeForCook).
- v84 meals in canonical order (display-only).
- v85 **Import from Photo** (Claude vision, no-fabrication, photo→imageUrl) + import transparency wording.
- v86 recipe library **group-by** (All/By chef/Recently added).
- (v82 earlier: meal-level cook button, notes off cards, grouped ingredients, step-prefix strip.)
- Stale list items verified already-done: #7 name-wrap + #8 AI/Send overlap (v72); #14/#15 cook photos (v71). Not re-done.

**BRANCH `overnight-data-risk` (merged to live in v93):**
- **#1** menuLibrary echo guard — `_recentlyDeletedRecipeIds` (30s TTL) so a stale snapshot can't resurrect a deleted recipe; `deleteFromMenuLibrary` records id + removes locally.
- **#3** `window.cleanupDuplicateRecipes()` — DRY-RUN by default; lists dup groups (key = findExistingLibraryRecipe), keeps richest; deletes only on `cleanupDuplicateRecipes({apply:true})`.
- **#6** cook photo stamped at assign time — `doAssignDish` uses `resolvePersonProfile` so `assignedToPhoto` travels to all devices.
- **#10 (diag only)** `window.diagGroceryItem('orange juice')` — non-destructive; shows per-dish scaled qty/unit + the `normKey` merge key, and counts distinct keys (>1 = split-quantity root cause).

**AM checklist (live):** hard-refresh to v86. (1) cooking a plan dish with steps no longer blanks; (2) ingredient sort toggle works in detail view; (3) meals show in canonical order; (4) 📷 Photo import a recipe photo → review → saves with the photo as image; (5) library group-by toggle; (6) `window.runRegressionTests()` = 25/25.
